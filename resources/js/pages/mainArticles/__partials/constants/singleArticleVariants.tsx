import { SingleArticleTypes } from '@/types';

const singleArticleVariants: SingleArticleTypes = {
    main: {
        className: `text-[25px]`,
    },
    other: {
        className: `text-[17px]`,
    },
};

export default singleArticleVariants;
